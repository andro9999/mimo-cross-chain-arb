version: "3.8"

services:
  mimo-arb:
    build: .
    container_name: mimo-cross-chain-arb
    ports:
      - "8080:8080"
    environment:
      - LLM_BASE_URL=${LLM_BASE_URL:-http://host.docker.internal:20128/v1}
      - LLM_MODEL=${LLM_MODEL:-xmtp/mimo-v2.5-pro}
      - LLM_API_KEY=${LLM_API_KEY:-sk-hermes}
    restart: unless-stopped
